<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.puri.mapper.PuriMapper">
	<!-- 회원가입 -->
	<insert id="SignUp" parameterType="kr.puri.entity.Member">
		insert into member
		values(#{id}, #{pw}, #{nick}, #{gender}, #{age})
	</insert>

	<select id="Login" resultType="kr.puri.entity.Member"
		parameterType="kr.puri.entity.Member">
		select * from Member where id=#{id} and pw=#{pw}
	</select>

	<select id="memberList" resultType="kr.puri.entity.Member">
		select * from Member
	</select>

	<select id="idCheck" parameterType="kr.puri.entity.Member" resultType="String">
		SELECT count(*) FROM member WHERE id = #{id}
	</select>
	
	<insert id="q_anwerInsert" parameterType="kr.puri.entity.Q_Answer">
      insert into question values(question_seq.nextval, #{question_1}, #{question_2}, #{question_3}, #{question_4}, #{question_5}, #{question_6}, #{question_7}, #{id})
   </insert>
	

</mapper>