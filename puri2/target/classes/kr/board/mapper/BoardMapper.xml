<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.board.mapper.BoardMapper">    
  <select id="boardList" resultType="kr.board.entity.Board">
    select * from board
  </select>
  
	<insert id="boardInsert" parameterType="kr.board.entity.Board">
     insert into board(idx, memId, title, content, writer)
     values(board_idx.nextval, #{memId}, #{title},#{content},#{writer})        
   </insert>
   
	<select id="boardContent" resultType="kr.board.entity.Board" parameterType="int">
	    select * from board where idx = #{idx}
	</select>
	
	<update id="boardUpdate" parameterType="kr.board.entity.Board">
	    update board set title=#{title}, content=#{content} where idx=#{idx}
	</update>
	
	<update id="boardUpdate1" parameterType="kr.board.entity.Board">
	    update board set title=#{title}, writer=#{writer} where idx=#{idx}
	</update>
	
	<select id="memberLogin" parameterType="kr.board.entity.Member" resultType="kr.board.entity.Member">
        select * from mem_tbl where memId=#{memId} and memPwd=#{memPwd}
    </select>
	
</mapper>